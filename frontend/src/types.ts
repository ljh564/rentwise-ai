export type Destination={label:string;address:string;weight:number;max_minutes:number};
export type Preferences={city:string;districts:string[];monthly_rent_max:number;monthly_total_max:number;bedrooms_min:number;area_min:number;rental_type:'entire'|'shared'|'either';move_in_date:string;lease_months:number;accepts_agent_fee:boolean;needs_elevator:boolean;allows_pets:boolean;commute_mode:'transit'|'driving'|'walking'|'bicycling';destinations:Destination[];soft_preferences:string[]};
export type Recommendation={listing:{id:string;title:string;district:string;neighborhood:string;address:string;monthly_rent:number;bedrooms:number;area_sqm:number;floor:number;has_elevator:boolean;allows_pets:boolean;image_url:string;source_name:string;source_url:string;tags:string[]};monthly_true_cost:number;first_month_cash:number;weighted_commute_minutes:number;worst_commute_minutes:number;weekly_total_commute_minutes:number;commute_fairness_gap_minutes:number;hard_constraints_passed:boolean;score:number;reasons:string[];tradeoffs:string[];commutes:{destination:string;minutes:number;distance_km:number;within_limit:boolean}[]};
export type SearchResponse={search_id:string|null;agent_run_id:string|null;provider:string;total_candidates:number;recommendations:Recommendation[];assumptions:string[]};
